import 'package:flutter/material.dart';
import '../../models/coupon_model.dart';

class CouponsListWithShopId extends StatelessWidget {
  final CouponModel couponsList;
  CouponsListWithShopId(this.couponsList);
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        
      ],
    );
  }
}
